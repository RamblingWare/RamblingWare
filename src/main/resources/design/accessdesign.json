{
  "_id": "_design/accessdesign",
  "views": {
    "users": {
      "map": "function (doc) {\n  if(doc.doctype != undefined && doc.doctype == \"user\") {\n    emit(doc._id, doc.name);\n  }\n}"
    },
    "roles": {
      "map": "function (doc) {\n  if(doc.doctype != undefined && doc.doctype == \"role\") {\n    emit(doc._id, doc.name);\n  }\n}"
    }
  },
  "language": "javascript"
}